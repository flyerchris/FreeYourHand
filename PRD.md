# PRD: FreeYourHand (Mac 語音輸入潤稿工具)

## 1. 產品定位
這是一個針對 Mac 用戶開發的高效語音輸入工具。它結合了 Apple Silicon 優化的本地 Whisper 辨識技術與 Gemini API 的校對能力，實現「語音輸入、精確還原、即時貼上」的無縫體驗。

## 2. 核心功能
### 2.1 語音輸入觸發
- **觸發按鍵**：預設為長按 **右側 `Option` 鍵 (Right Option)**。
- **長按模式**：使用者長按預設快捷鍵開始錄音，放開後自動停止並處理。
- **翻譯模式**：長按主要快捷鍵時，若同時按住 **`Shift`**，則將辨識內容翻譯為英文輸出。
- **自定義功能**：使用者可在設定視窗中自行修改觸發按鍵。

### 2.2 視覺回饋 (UI)
- **聲紋動畫**：錄音時在畫面下方顯示動態能量條（Sound Waveform）。
- **處理狀態**：辨識與 API 呼叫期間，原處顯示載入動畫（轉圈圈）。
- **模型管理**：初次啟動或切換模型時，顯示模型下載進度條，避免黑箱操作感。

### 2.3 AI 處理流水線
1. **辨識層**：使用 `mlx-whisper` 在本地端進行語音轉文字（支援中英）。
2. **處理層 (精確還原模式)**：
   - **一般模式**：透過 Gemini API 修正逐字稿。重點在於修正同音異義字、補上標點符號、移除語氣贅詞（如「呃」、「那個」），**嚴禁改寫使用者的語氣或風格**，目標是百分之百還原使用者的真實表達。
   - **翻譯模式**：將內容精確翻譯並優化為英文。
3. **輸出層**：將最終文字寫入剪貼簿，並自動執行 `Cmd + V` 貼上至當前游標處。

### 2.4 設定與日誌
- **設定視窗**：
  - **Gemini 連線設定**：支援使用 **API Key** 或 **OAuth 授權**。
  - **模型管理**：選擇 Whisper 模型版本，並查看下載/存放狀態。
  - **快捷鍵自定義**。
  - **自定義校對指令 (Prompt)**。
- **日誌功能**：內建日誌查看器，記錄辨識結果、API 狀態與系統錯誤。

## 3. 技術規格與部署
- **開發語言**：Python 3.10+
- **GUI 框架**：PyQt6。
- **部署方式**：
  - **開發模式**：支援直接執行 Python 腳本，方便快速測試。
  - **發佈模式**：使用 `py2app` 打包成獨立的 `FreeYourHand.app`。
- **權限需求**：錄音權限 (Microphone)、輔助使用權限 (Accessibility)。

## 4. 使用者流程
1. 啟動 App -> 檢查模型（若無則引導下載）-> 檢查權限 -> 設定 Gemini。
2. 在任何輸入框按下快捷鍵 -> 看到聲紋動畫 -> 說話。
3. 放開按鍵 -> 顯示處理中 -> 文字精確出現在游標處。
